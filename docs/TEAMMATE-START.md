# Active teammate handoff

The current human collaborator is **sbardacosta-code**. Own the demo rehearsal in
[GitHub issue #1](https://github.com/laulpogan/haasome/issues/1): a proposed 90-second
script, a first-time browser walkthrough, and up to three presentation problems.
Start with 30 minutes. An issue comment is a complete handoff; coding is optional.

The issue contains a copy-paste Codex prompt and an observation template. If writing
a document, own only `docs/DEMO-REHEARSAL.md` on a separate branch based on
`origin/goals/quality-and-object-memory`. Do not merge. Keep private media out of GitHub.

The local demo address works only on the presentation Mac. Remote collaborators
need an approved bundle or a walkthrough; GPU setup is not part of this lane.

## Active technical owners

- Quality Astra: `pipelines/scene/`, branch `work/room-quality-native`.
- Object-memory Astra: `apps/palace/`, branch `work/object-memory-surface`.
- Collection Astra: `services/memory/`, branch `work/selected-context-austin`.
- Euler: independent curated fallback, branch `work/curated-spatial-demo`.
- Coordinator: root docs, shared contracts, integration and final rehearsal,
  branch `integration/demo-polish`.

See [quality goals](QUALITY-GOALS.md) for the technical finish lines. These branches
name ownership, not completed or integrated capabilities. The coordinator chooses
the final scene after inspecting results and verifies the full capsule flow.

## Handoff format

Return commit (if any), run command, input/output paths, observed result and blocker.
Separate proposed narration from rehearsed behavior. Report manual placement as
manual; report computer use only with observed source capture. A licensed setting
must retain its attribution. Recognition remains pending until the running viewer
consumes and preserves actual object bindings.
